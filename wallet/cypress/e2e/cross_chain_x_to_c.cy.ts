describe('Cross chain: X to C', () => {
  context('normal cases: ', () => {
    it('export CAM from X to C', () => {
      cy.switchToWalletApp().changeNetwork().accessWallet('mnemonic')
    })
  })
})